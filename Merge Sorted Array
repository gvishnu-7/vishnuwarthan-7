#include <stdio.h>

void merge(int* nums1, int nums1Size, int m, int* nums2, int nums2Size, int n) {
    int i = m - 1;         // Index of the last valid element in nums1
    int j = n - 1;         // Index of the last element in nums2
    int k = m + n - 1;     // Index of the last slot in the combined nums1 array
    
    // Compare elements from the back of both arrays and place the larger one at index k
    while (i >= 0 && j >= 0) {
        if (nums1[i] > nums2[j]) {
            nums1[k] = nums1[i];
            i--;
        } else {
            nums1[k] = nums2[j];
            j--;
        }
        k--;
    }
    
    // If elements are left over in nums2, copy them over.
    // Note: If elements are left in nums1, they are already in their correct places!
    while (j >= 0) {
        nums1[k] = nums2[j];
        j--;
        k--;
    }
}
